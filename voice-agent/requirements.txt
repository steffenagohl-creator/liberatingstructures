# LS-Voice-Agent — LiveKit-Worker (🇪🇺 Mistral/Voxtral). Offizielle Plugins, kein Klara-Code.
livekit-agents~=1.5
livekit-plugins-mistralai~=1.5
livekit-plugins-silero~=1.5
# 🇺🇸-Pfad (OpenAI Realtime) — Code/Abhängigkeit vorbereitet, aktiv erst mit OPENAI_API_KEY:
livekit-plugins-openai~=1.5
python-dotenv>=1.0,<2
httpx>=0.27,<1.0
